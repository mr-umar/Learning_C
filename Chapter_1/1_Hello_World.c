#include <stdio.h> /* "#include" is a reserved term to import code, in this case to import standard input/output library */

main(){     /* "main" another reserved term, the compiler starts from main, the start point of my program */
    printf("Hello World\n"); /* printf = print format */
}

/* to exec "gcc 1_Hello_World.c -o 1_Hello_World"  */